# TODO - Neo4j Aura kết nối

- [ ] Update `src/main.py` để ưu tiên dùng `NEO4J_URI` (ví dụ `neo4j+s://...`) và `NEO4J_USERNAME/NEO4J_PASSWORD`.
- [ ] Kiểm tra lại format URI fallback về `bolt://` nếu không có `NEO4J_URI`.
- [ ] Chạy thử script (ít bản ghi) để xác nhận kết nối Neo4j Aura.

